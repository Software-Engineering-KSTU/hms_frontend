# 🚀 HMS WEB

## 🛠️ Установка Flutter SDK

Для локальной разработки необходимо установить Flutter SDK и настроить переменные среды.

### Шаг 1: Проверка системных требований

Убедитесь, что у вас установлены:
1.  **Git** (необходим для работы Flutter).
2.  **Flutter SDK** (для Windows/macOS/Linux).
3.  **IDE:** Android Studio.

### Шаг 2: Установка Flutter

Выберите вашу операционную систему и следуйте инструкциям:

#### 💻 Windows

1.  **Загрузка:** Скачайте последнюю стабильную версию Flutter SDK в виде ZIP-архива с [официального сайта Flutter](https://docs.flutter.dev/get-started/install/windows).
2.  **Распаковка:** Распакуйте архив в папку, например, `C:\flutter\`. (Избегайте `C:\Program Files\`).
3 Нажми Win + R, введи sysdm.cpl → Enter.
4. Перейди во влкадку Дополнительно → нажми на кнопку "Переменные среды".
5. В разделе Переменные пользователя выбери Path → Изменить → Создать и вставь путь к папке flutter\bin:

```
C:\src\flutter\bin #Например или туда куда вы вставили
```

6. Повтори то же для Системных переменных, чтобы Flutter был доступен всем пользователям.
7. Нажми OK во всех окнах.

Проверка:

``` Для проверки нажмите Win+r и введите cmd```

Вам откроется командая строка и введите следующую команду:

```
flutter --version
```

Должна отобразиться версия Flutter и Dart.

#### 🍎 macOS

1.  **Загрузка:** Скачайте последнюю стабильную версию Flutter SDK с [официального сайта Flutter](https://docs.flutter.dev/get-started/install/macos).
2.  **Распаковка:** Распакуйте архив в желаемое место, например, в `$HOME/developer/`.
3.  **PATH:** Добавьте Flutter в переменную PATH, выполнив в терминале (в зависимости от вашей оболочки, `zsh` или `bash`):

4.  ```brach
    vim .zshrc
    ```
     далее добваить туда:
    ```bash
    export PATH="$PATH:$HOME/developer/flutter/bin" #указать путь до flutter/bin
    ```
5.  **Применение:** Примените изменения: `source ~/.zshrc` (или `.bashrc`).
   
### Шаг 3: Проверка установки

В терминале, находясь в папке проекта:

Проверка:

```flutter doctor```

После настройки переменной PATH, откройте терминал или командную строку и выполните:

Убедись, что Flutter SDK корректно указан и все необходимые компоненты установлены.

Проверка вывода на наличие всех необходимых компонентов:

Должны быть зелёные галочки ✅ напротив Flutter, Android toolchain, VS Code/Android Studio, Connected device (если есть устройства).

Если есть ошибки или предупреждения ⚠️, их нужно исправить (например, добавить Android SDK или согласовать лицензию).

Примечание: должен быть установлене обязательно ```chorome``` браузер
